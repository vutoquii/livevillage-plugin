name: LiveVillage
version: ${project.version}
main: com.vutocorp.livevillage.LiveVillagePlugin
api-version: '1.21.4'
# /lv se registra por Brigadier (LvBrigadier), no aqui: declararlo tambien en
# "commands:" hace que Paper avise de un comando duplicado.
